{
  "name": "mocha-selenium-sample",
  "version": "0.0.1",
  "description": " Selenium examples for Mocha and LambdaTest",
  "scripts": {
    "test": "npm run single && npm run parallel",
    "single": "mocha specs/single_test.js"
  },
  "keywords": [
    "mocha",
    "LambdaTest",
    "selenium",
    "examples"
  ],
  "dependencies": {
    "bluebird": "^3.4.6",
    "chromedriver": "^103.0.0",
    "mocha": "^9.1.3",
    "selenium-webdriver": "^4.3.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.1"
  }
}
